# Set the default registry
registry=https://registry.npmjs.org/

# Set the default package manager for your project (e.g., npm, yarn)
# Using npm here; feel free to adjust based on your choice.
package-lock=true

# Save exact versions of installed packages in package.json
save-exact=true

# Set the cache folder location
cache=~/.npm

# Configure the auth token for private registries (replace with your actual token)
# _authToken=your-auth-token-here

# Configure proxy if you're behind a corporate firewall (optional)
# proxy=http://proxy.example.com:8080
# https-proxy=http://proxy.example.com:8080

# Enable workspaces (if you are using npm workspaces in a monorepo)
# workspace=true

# Specify the location of the lock file for monorepos
# lockfile=package-lock.json

# Always install the latest version of npm for new projects
# npm-version=latest

# Set default CLI to silent or verbose
# loglevel=warn
