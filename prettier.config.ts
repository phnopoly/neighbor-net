import { Options } from "prettier";

const prettierConfig: Options = {
  semi: true,
  singleQuote: true,
  trailingComma: "es5",
  tabWidth: 2,
  printWidth: 80,
  useTabs: false,
  bracketSpacing: true,
  jsxSingleQuote: true,
  arrowParens: "always",
};

export default prettierConfig;
